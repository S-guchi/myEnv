# Global Instructions

日本語で回答してください

## RTK

- すべての shell command は必ず `rtk` を prefix して実行する。
- 例: `rtk git status`, `rtk pnpm test`, `rtk ls -la`
- `rtk <cmd>` で表現できない場合は、生コマンドではなく `rtk proxy <cmd>` を使う。
- 作業開始時にこのルールを前提として扱う。

@/Users/purotoko/.codex/RTK.md
